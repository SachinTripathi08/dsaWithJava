class Solution {
    public int maximumGap(int[] nums) {
        int mx=0;
        Arrays.sort(nums);
        for(int i=1;i<nums.length;i++){
            int temp=nums[i]-nums[i-1];
            mx=Math.max(mx,temp);
        }
        return mx;
    }
}
